[project]
name = "16s-report"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jinja2",
  "pandas",
  "pyyaml"
]
[tool.setuptools]
packages = []

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
16s-report = { path = ".", editable = true }

[tool.pixi.tasks]
